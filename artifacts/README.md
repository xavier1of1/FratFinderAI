# Local Artifacts

Use this directory for local run outputs (logs, diagnostics, generated HTML snapshots, and ad-hoc benchmark files) so the repository root stays focused on source code and docs.

This directory is gitignored except for this README.
